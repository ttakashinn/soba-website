---
import { getLangFromUrl, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);

const badges = {
  vi: [
    { icon: '✅', text: '50+ Dự án hoàn thành', color: 'blue' },
    { icon: '😊', text: '98% Khách hàng hài lòng', color: 'green' },
    { icon: '📅', text: '5+ Năm kinh nghiệm', color: 'purple' },
    { icon: '🕐', text: 'Support 24/7', color: 'orange' },
    { icon: '🔒', text: 'Bảo mật SSL', color: 'red' },
    { icon: '📝', text: 'NDA ký kết', color: 'indigo' }
  ],
  en: [
    { icon: '✅', text: '50+ Projects Completed', color: 'blue' },
    { icon: '😊', text: '98% Client Satisfaction', color: 'green' },
    { icon: '📅', text: '5+ Years Experience', color: 'purple' },
    { icon: '🕐', text: '24/7 Support', color: 'orange' },
    { icon: '🔒', text: 'SSL Security', color: 'red' },
    { icon: '📝', text: 'NDA Signed', color: 'indigo' }
  ],
  ja: [
    { icon: '✅', text: '50以上のプロジェクト完了', color: 'blue' },
    { icon: '😊', text: '98%の顧客満足度', color: 'green' },
    { icon: '📅', text: '5年以上の経験', color: 'purple' },
    { icon: '🕐', text: '24時間365日サポート', color: 'orange' },
    { icon: '🔒', text: 'SSLセキュリティ', color: 'red' },
    { icon: '📝', text: 'NDA締結', color: 'indigo' }
  ]
};

const currentBadges = badges[lang as keyof typeof badges] || badges.vi;
---

<section class="py-12 bg-white border-y border-gray-100">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Mobile: Horizontal scroll -->
    <div class="flex lg:hidden gap-4 overflow-x-auto pb-4 -mx-4 px-4 scrollbar-hide">
      {currentBadges.map((badge) => (
        <div class="flex-shrink-0 flex items-center gap-3 bg-gray-50 hover:bg-gray-100 rounded-full px-5 py-3 transition-colors">
          <span class="text-2xl">{badge.icon}</span>
          <span class="text-gray-700 font-medium whitespace-nowrap">{badge.text}</span>
        </div>
      ))}
    </div>

    <!-- Desktop: Grid -->
    <div class="hidden lg:grid grid-cols-6 gap-4">
      {currentBadges.map((badge) => (
        <div class="group flex flex-col items-center text-center p-4 rounded-xl hover:bg-gray-50 transition-all duration-300 cursor-default">
          <div class="text-4xl mb-2 grayscale group-hover:grayscale-0 transition-all duration-300 group-hover:scale-110">
            {badge.icon}
          </div>
          <span class="text-sm text-gray-600 group-hover:text-gray-900 font-medium transition-colors">
            {badge.text}
          </span>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .scrollbar-hide {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }
</style>
